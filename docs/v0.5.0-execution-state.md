# v0.5.0 Execution State

> Temporary construction ledger. Remove before the `v0.5.0` tag.

## Release contract

- Branch: `codex/v0.5.0`
- Base: `v0.4.2` / `26fb59e288e781196fd51154e2239bcd15a48105`
- Final tag: annotated `v0.5.0`
- Publish: push `main`, push tag, wait for all GitHub Actions jobs, verify GitHub Release and GHCR.
- Excluded: production-server login, deployment, or production pricing-file replacement.
- Default `pricing.yaml` is the copyable release input for the operator.

## Frozen decisions

- `grok-4.5`: flat input `2.00`, cached input `0.30`, output `6.00` per 1M; no unverified long-context threshold.
- Generic long-context support remains required and is validated with models that have a known threshold, including Gemini 3.1 Pro at 200,000 input tokens.
- Temporary `AGENTS.md` and this ledger must not exist in the release tree.

## Baseline evidence

- Current green head: `6aa44ef`
- `go test ./... -count=1`: pass after metrics integration
- `go vet ./...`: pass after metrics integration
- `go test -race ./internal/metrics ./internal/report -count=1`: pass
- `go test -race ./internal/db -count=1`: pass
- `bash -n scripts/backup.sh` via Git for Windows: pass
- Git push dry-run: pass
- Docker client/server: 29.6.1
- `v0.5.0`: absent locally and remotely
- Reviewed million-row Key benchmark: pass; exact-Key reports <= 66 ms at
  30d, global Keys remains the documented 14.09 s boundary

## Phase state

| Phase | State | Evidence / next action |
|---|---|---|
| 0 Contract and guardrails | complete | `b77e6bf`; plan, temporary contract, ledger, and pricing decision committed |
| 1 Reporting Core | complete | Read/write DTO split, narrow readers, typed core reports, context propagation, no wide `ReportStore` |
| 2 Cost and pricing | complete | One homogeneous request-level bucket path serves every cost-bearing report; strict tier/image pricing is enabled and Gemini 3.1 Pro 200k tier is active in `pricing.yaml` |
| 3 Key DB and API | complete | Key aggregate/scope, Issues isolation, exact API contract, composite index, corrected million-row evidence |
| 4 Key WebUI and CLI | complete | Key Detail/i18n/a11y plus strict `validate` and stdin-only `hash-key` |
| 5 Production closeout | in progress | health/ready, Docker healthcheck, report metrics, demo, release attachments, docs, and migration/rollback rehearsal complete; performance/privacy/live smoke + full RC remain |
| RC and release | pending | Full gates, cleanup, main/tag push, Actions/Release/GHCR verification |

## Active work

1. ~~Complete README and release notes for Key workflow, partial/current-pricing
   semantics, strict CLI, health/readiness, non-default Docker health URL,
   upgrade, migration, and rollback.~~ **docs closeout done in this slice**
   (`README.md`, `docs/v0.5.0-release-notes.md`; config/pricing audited, no
   defect requiring change).
2. ~~Run a real `v0.4.2` DB upgrade, interrupted-index recovery, and old-binary
   plus old-pricing rollback rehearsal; preserve evidence.~~ **complete**; see
   `docs/v0.5.0-migration-rehearsal.md`.
3. Run proxy baseline/concurrent-report benchmarks, privacy audit, and local
   desktop/mobile + language/theme live smoke.
4. Complete full RC: test, full race, vet, build, Docker build/healthcheck, and
   artifact/config validation.
5. Remove `AGENTS.md` and this ledger, merge/push `main`, create/push annotated
   `v0.5.0`, then verify all Actions, Release attachments, and GHCR tags.
6. Keep the original checkout's ignored SQLite, salt, executable, and
   development config untouched.

## Docs closeout evidence (this slice)

- README: removed stale `v0.4.0` / bare `-config` host-network examples; expanded
  production checklist; repo map includes `internal/report` and write-side
  event/store; demo-label semantics; data compatibility for additive Key index;
  known limit documents global 30d Keys ~14.09 s and exact-Key <=66 ms; strict
  CLI/health/readiness/upgrade/full-bundle rollback and current-pricing
  re-estimation language retained/clarified; Caddy Basic Auth/equivalent
  required in checklist and limits.
- Release notes: `docs/v0.5.0-release-notes.md` written as annotated-tag source;
  Gemini 3.1 Pro is pricing/tier test example only; Grok 4.5 flat
  `2.00 / 0.30 / 6.00` with no long-context tier.
- `config.yaml` / `pricing.yaml` audit against frozen plan: no release-facing
  defect found; left unchanged.

## Migration rehearsal evidence

- Actual v0.4.2 tag binary created the schema and salt binding for a synthetic
  1,000,000-row, 100-Key, 100-model, 30-day fixture.
- Clean v0.5.0 upgrade: 1,544 ms to self-check; row count and integrity
  preserved; composite index present; checkpointed growth 80,384,000 bytes.
- Forced termination after 5,397,232 WAL bytes: SQLite rolled back the
  uncommitted index atomically; integrity and 1,000,000 rows remained intact;
  restart rebuilt the index successfully in 1,575 ms.
- Rollback: v0.4.2 binary with tag-matching v0.4.2 pricing started on the
  upgraded DB in 84 ms; integrity, row count, schema version, and extra index
  all remained valid.
- Full method, provenance, caveats, and exact results are preserved in
  `docs/v0.5.0-migration-rehearsal.md`.
