# v0.5.0 Execution State

> Temporary construction ledger. Remove before the `v0.5.0` tag.

## Release contract

- Branch: `codex/v0.5.0`
- Base: `v0.4.2` / `26fb59e288e781196fd51154e2239bcd15a48105`
- Final tag: annotated `v0.5.0`
- Publish: push `main`, push tag, wait for all GitHub Actions jobs, verify GitHub Release and GHCR.
- Excluded: production-server login, deployment, or production pricing-file replacement.
- Default `pricing.yaml` is the copyable release input for the operator.

## Frozen decisions

- `grok-4.5`: flat input `2.00`, cached input `0.30`, output `6.00` per 1M; no unverified long-context threshold.
- Generic long-context support remains required and is validated with models that have a known threshold, including Gemini 3.1 Pro at 200,000 input tokens.
- Temporary `AGENTS.md` and this ledger must not exist in the release tree.

## Baseline evidence

- `go test ./... -count=1`: pass
- `go test -race ./... -count=1`: pass
- `go vet ./...`: pass
- `bash -n scripts/backup.sh` via Git for Windows: pass
- Git push dry-run: pass
- Docker client/server: 29.6.1
- `v0.5.0`: absent locally and remotely

## Phase state

| Phase | State | Evidence / next action |
|---|---|---|
| 0 Contract and guardrails | complete | `b77e6bf`; plan, temporary contract, ledger, and pricing decision committed |
| 1 Reporting Core | in progress | Models, Summary, Timeseries, Images, Overview, Model Assets, Keys, Activity, and Requests use typed report boundaries; Issues is the active migration |
| 2 Cost and pricing | complete | One homogeneous request-level bucket path serves every cost-bearing report; strict tier/image pricing is enabled and Gemini 3.1 Pro 200k tier is active in `pricing.yaml` |
| 3 Key DB and API | in progress | Labels, complete Key aggregate, and Models/Timeseries/Activity/Requests exact filters are complete; Issues, index, and API contract remain |
| 4 Key WebUI and CLI | pending | Detail workflow, i18n, hash-key, validate |
| 5 Production closeout | pending | health/ready, metrics, demo, docs, migration, performance |
| RC and release | pending | Full gates, cleanup, main/tag push, Actions/Release/GHCR verification |

## Active work

Green head before the active Grok slice: `9932709`.

1. Finish typed Issues, exact Key scope, explicit optional-source status, and cancellation/error handling.
2. Remove the remaining read-side `event` DTOs and wide `store.ReportStore` dependency without changing API success shapes.
3. Complete exact API routing/error/method contracts, then Key Detail, CLI, health/readiness, metrics, and the Key index/performance gates.
4. Keep the original checkout's ignored SQLite, salt, executable, and development config untouched.
